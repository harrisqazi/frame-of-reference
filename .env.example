# Copy to .env.local for local testing only — do NOT commit real keys to GitHub.
# For production, set these in Vercel → Settings → Environment Variables.

# Required for email to admin@pooly.org
RESEND_API_KEY=re_your_key_here
RESEND_FROM=Frame of Reference <onboarding@resend.dev>
NOTIFY_EMAIL=admin@pooly.org

# Optional Google Sheets backup
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=
SPREADSHEET_ID=
